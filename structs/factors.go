package structs

func FindFactors(num int) []int {
	// n := 1
	// partner := num / 2
	// factors := []int{}
	return []int{}
}

/*

   let n = 1
   let Set = {}
   while (n <= math.sqrt(num)) {
       if (num % n === 0) {
           partner := num / n;
           add n and partner to Set
       }
       n++
   }
*/
